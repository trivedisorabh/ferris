describe('TextField', () => {
	// it('has a default text "Hello World!"', async () => {
	// 	const el = await fixture<TextField>(html`<skf-text-field></skf-text-field>`);
	// 	await expect(el.label).to.equal('');
	// });
	// it('can override the name via attribute', async () => {
	// 	const label = 'Bob';
	// 	const el = await fixture<TextField>(html`<skf-text-field label=${label}></skf-text-field>`);
	// 	await expect(el.label).to.equal(label);
	// });
	// it('passes the a11y audit', async () => {
	// 	const el = await fixture<TextField>(
	// 		html`<skf-text-field id="text-field-test" label="Lorem ipsum"></skf-text-field>`
	// 	);
	// 	await expect(el).shadowDom.to.be.accessible();
	// });
});
