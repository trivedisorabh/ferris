export interface LineHeightProps {
	compact: string;
	normal: string;
	reset: string;
}

const LineHeights: LineHeightProps = {
	compact: '1.25',
	normal: '1.5',
	reset: '1',
};

export default LineHeights;
